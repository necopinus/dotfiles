{ config, pkgs, ... }:

{
  # imports = [
  #   ../programs/foo.nix
  # ];

  # home.packages = with pkgs; [
  #   some-tool
  # ];

  # programs.example = {
  #   enable = true;
  # };

  # home.file = {
  #   ".config.foo".source = ../artifacts/config.foo;
  # };

  # home.sessionVariables = {
  #   FOO_VARIABLE = "bar";
  # };
}
