# Include any host-specific configurations
#
Include hosts/*

# Make sure gpg-agent prompts on the right TTY
#
#Match host * exec "gpg-connect-agent updatestartuptty /bye"

# Defaults
#
Host * !127.0.0.1 !localhost
	AddKeysToAgent      yes
	Compression         yes
	ForwardAgent        no
	ServerAliveInterval 15
	ServerAliveCountMax 4
